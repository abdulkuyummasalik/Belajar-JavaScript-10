// // #2
// // Menerima input angka dari pengguna
// const angka = parseInt(prompt("Masukkan sebuah angka (tidak nol):"));

// // Memeriksa apakah angka valid (tidak nol)
// if (angka !== 0 && !isNaN(angka)) {
//   // Memeriksa apakah angka positif atau negatif
//   const jenisPosisi = angka > 0 ? "positif" : "negatif";

//   // Memeriksa apakah angka genap atau ganjil
//   const jenisKelipatan = angka % 2 === 0 ? "genap" : "ganjil";

//   // Menampilkan hasil
//   console.log(`Angka ${angka} adalah ${jenisPosisi} dan ${jenisKelipatan}.`);
// } else {
//   console.log("Masukkan angka yang valid (tidak nol).");
// }

// let angka = prompt('masukan angka');
// if ((angka %= 0)) {
//   console.log(angka + " adalah bilangan Genap");
// } else {
//   console.log(angka + " adalah bilangan Ganjil");
// }
